---
title: "The Ghost Endpoint (404 & 400)"
date: "2026-02-11"
description: "API 문 앞에서 '열려라 참깨'만 100번 외친 사연. (feat. Cloud Code API)"
tags: ["Error"]
logId: "ERR-003"
---

![Cover](/d9log/images/2026-02-11-ghost-endpoint.jpg)

## 👻 404 Not Found의 공포

어제 개발 중 겪은 에러 로그를 보면 아직도 등골이 서늘하다.<br/>
분명 서버는 살아있는데, 문을 안 열어준다.

```
[Antigravity] Probing endpoint: https://cloudcode-pa.googleapis.com/v1internal:chat
[Antigravity] Failed: 404 Not Found
```

`:chat`이 아니면 뭐지?<br/>
`:generateContent`? `:streamGenerateContent`?<br/>
다 찔러봤다. 결과는 **400 Bad Request** 아니면 **404**.

### 🧩 400 Bad Request: 희망고문

404는 "그런 거 없다"니까 포기하면 되는데, **400**은 사람을 미치게 한다.<br/>
**"있긴 있는데, 니가 보낸 데이터가 맘에 안 들어."**

```json
{
  "error": {
    "code": 400,
    "message": "Invalid JSON payload received. Unknown name \"contents\": Cannot find field."
  }
}
```

아니, Gemini 표준 필드가 `contents`인데 그걸 모른다고?<br/>
우리는 `contents`, `messages`, `prompt` 온갖 필드명을 다 시도해봤지만 실패했다.

### 🕵️‍♂️ 비밀은 "Payload Wrapping"

알고 보니 문제는 **Payload 구조**에 있었다.<br/>
Cloud Code API는 일반적인 Gemini API와 달리, 요청 본문을 한 번 더 감싸야 했다.

**틀린 Payload:**
```json
{ "contents": [...] }
```

**맞는 Payload:**
```json
{
  "model": "gemini-3-pro",
  "project": "...",
  "request": {
    "contents": [...] 
  }
}
```

저 `request`라는 래퍼(Wrapper) 하나 때문에 2시간을 헤맸던 것이다.<br/>
API 문서는 없고, 로그는 불친절하고... 결국 **집요한 코드 분석과 추론**만이 답이었다.

**에러는 나침반이다.**<br/>
400이 뜨면 보따리(Payload)의 매듭을 다시 묶어라.<br/>
정답은 언제나 디테일에 숨어 있다.
